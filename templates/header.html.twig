<header>
    <nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Sortir ENI</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link {{ app.request.get('_route') == 'event' ? 'active' :'' }}" href="{{path('event')}}">Accueil</a>
            </li>
            {% if app.user.isAdmin %}
            <li class="nav-item">
                <a class="nav-link" href="#">Villes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Campus</a>
            </li>
            {% endif %}
            <li class="nav-item">
                <a class="nav-link {{ app.request.get('_route') == 'participant_edit' ? 'active' :'' }}" href="{{path('participant_edit')}}">Mon profil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {{ app.request.get('_route') == 'app_logout' ? 'active' :'' }}" href="{{path('app_logout')}}">Se deconnecter</a>
            </li>
            </ul>
            {# <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
            </form> #}
        </div>
    </nav>
    <div style="height: 60px;"></div>
</header>